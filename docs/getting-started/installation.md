# Installation

Artistry can be installed by adding the following code into your `config/extra.toml` file:

```toml
[[ballsdex.packages]]
location = "git+https://github.com/Caylies/Artistry.git@1.1.0"
path = "artistry"
enabled = true
```

Once added, you may now rebuild your bot and run it!
